name: doctry
description: "DOCTRY — plateforme intelligente de gestion, perte et retrouvaille de documents d'identité."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  http: ^1.6.0
  shared_preferences: ^2.5.3
  image_picker: ^1.2.1
  intl: ^0.20.2
  path_provider: ^2.1.5
  web: ^1.1.1
  mobile_scanner: ^7.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
